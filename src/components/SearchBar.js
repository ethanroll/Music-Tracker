function SearchBar(){
    return(
        <div>
            <input type = "text" placeholder = "Search for an album..." />
            <button>Search</button>
        </div>
    );
}

export default SearchBar;